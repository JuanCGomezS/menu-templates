---
interface Props {
  contact?: {
    whatsapp?: string;
    instagram?: string;
    address?: string;
  };
}

const { contact } = Astro.props;

if (!contact) {
  return null;
}
---

<div class="mb-4">
  <h3 class="text-xl font-bold text-gray-900 mb-4 flex items-center">
    <span class="mr-2">📞</span>
    Contacto
  </h3>
  <div class="bg-gradient-to-br from-cyan-50 to-blue-50 rounded-lg p-5 border border-cyan-200">
    <ul class="list-none p-0 m-0 space-y-3">
      {contact.whatsapp && (
        <li class="flex items-center py-2">
          <span class="text-2xl mr-3">📱</span>
          <div>
            <span class="text-sm text-gray-600 font-medium">WhatsApp</span>
            <p class="text-gray-900 font-semibold">{contact.whatsapp}</p>
          </div>
        </li>
      )}
      {contact.instagram && (
        <li class="flex items-center py-2">
          <span class="text-2xl mr-3">📷</span>
          <div>
            <span class="text-sm text-gray-600 font-medium">Instagram</span>
            <p class="text-gray-900 font-semibold">{contact.instagram}</p>
          </div>
        </li>
      )}
      {contact.address && (
        <li class="flex items-center py-2">
          <span class="text-2xl mr-3">📍</span>
          <div>
            <span class="text-sm text-gray-600 font-medium">Dirección</span>
            <p class="text-gray-900 font-semibold">{contact.address}</p>
          </div>
        </li>
      )}
    </ul>
  </div>
</div>

